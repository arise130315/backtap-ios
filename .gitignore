# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.swiftpm/
Packages/
Package.pins
Package.resolved
.build/

# CocoaPods (if used later)
Pods/

# Carthage (if used later)
Carthage/Build/

# Local secrets (API keys etc.) — never commit
# 用 glob 避免文件夹改名时 ignore 失效
**/Secrets.swift

# Claude Code 本地状态(worktrees / 缓存等)
.claude/
