# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.swiftpm/
Packages/
Package.pins
Package.resolved
.build/

# CocoaPods (if used later)
Pods/

# Carthage (if used later)
Carthage/Build/

# Local secrets (API keys etc.) — never commit
SnapTranslate/SnapTranslate/Core/Analysis/Secrets.swift

# Claude Code 本地状态(worktrees / 缓存等)
.claude/
